# Copia este archivo como ".env" para desarrollo local si quieres apuntar
# a un backend distinto al de tu máquina (localhost:8080 es el valor por defecto).
#
# En Vercel, esta misma variable se configura en:
# Project Settings → Environment Variables
VITE_API_BASE_URL=http://localhost:8080
